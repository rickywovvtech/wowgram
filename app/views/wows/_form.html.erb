<%= form_with(model: wow, local: true) do |form| %>
  <% if wow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wow.errors.count, "error") %> prohibited this wow from being saved:</h2>

      <ul>
        <% wow.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>
  <div class="field">
    <%= form.label :Picture %>
    <%= form.file_field :picture %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
